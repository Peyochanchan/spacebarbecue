<div class="col-6 offset-3">
    <div class="d-block justify-content-center m-2 p-2">
    <%= simple_form_for [ @location, @review ] do |f| %>
        <%= f.input :content, label: "Text of your Review" %>
        <%= f.submit "Validate" %>
    <% end %>
    </div>
</div>