<div class="card-body col-6 border border-light shadow bg-body rounded" style="width: 18rem; margin-top: -15px !important;">
  <div id="map"
    style="width: 100%; height: 100%"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>
